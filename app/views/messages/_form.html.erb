<%= form_with model: Message.new, url: user_messages_path(@receiver), data: { turbo: true } do |f| %>
  <%= f.text_area :content, placeholder: "Type your message..." %>
  <%= f.submit "Send" %>
<% end %>
