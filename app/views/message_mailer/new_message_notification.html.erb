
<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <style>
      body {
        font-family: "Segoe UI", Roboto, "Helvetica Neue", sans-serif;
        background-color: #f5f7f6;
        margin: 0;
        padding: 0;
        color: #2e3a38;
      }

      .email-container {
        max-width: 640px;
        margin: 40px auto;
        background: #ffffff;
        border-radius: 10px;
        overflow: hidden;
        border: 1px solid #d1dfd8;
        box-shadow: 0 6px 18px rgba(0, 0, 0, 0.07);
      }

      .email-header {
        background-color: #14502b ; /* deep forest green */
        color: white;
        padding: 28px;
        text-align: center;
      }

      .email-header h1 {
        margin: 0;
        font-size: 24px;
        font-weight: 600;
      }

      .email-body {
        padding: 28px 26px;
        font-size: 16px;
        line-height: 1.7;
      }

      .email-body p {
        margin-bottom: 16px;
      }

      .message-block {
        background-color: #f1f7f4;
        padding: 16px;
        margin-bottom: 18px;
        border-left: 5px solid #14502b;
        border-radius: 5px;
      }

      .message-block p {
        margin: 5px 0;
      }

      .cta-button {
        display: inline-block;
        margin-top: 24px;
        padding: 12px 20px;
        background-color: #14502b;
        color: #ffffff;
        text-decoration: none;
        border-radius: 6px;
        font-weight: bold;
        font-size: 15px;
        transition: background-color 0.3s ease;
      }

      .cta-button:hover {
        background-color: #14502b;
      }

      .email-footer {
        background: #14502b;
        color: #ffffff;
        padding: 16px;
        text-align: center;
        font-size: 13px;
      }
    </style>
  </head>

  <body>
    <div class="email-container">
      <div class="email-header">
        <h1>You’ve Got New Messages!</h1>
      </div>

      <div class="email-body">
        <p>Hi <%= @receiver.full_name %>,</p>
        <p>You have <strong><%= @messages.count %></strong> unread message<%= 's' if @messages.count > 1 %> waiting for you:</p>

        <% @messages.each do |message| %>
          <div class="message-block">
            <p><strong>From:</strong> <%= message.sender.full_name %></p>
            <p><%= message.content %></p>
          </div>
        <% end %>

        <p style="text-align: center;">
          <a href="http://127.0.0.1:3000/messages" class="cta-button">View Messages</a>
        </p>
      </div>

      <div class="email-footer">
        Please don’t reply to this email
      </div>
    </div>
  </body>
</html>
