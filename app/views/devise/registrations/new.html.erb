<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
      /* Main Container Styling */
      .login-container {
          max-width: 400px;
          margin: 40px auto;
          background-color: rgba(255, 255, 255, 0.8);
          padding: 40px;
          border-radius: 8px;
          box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
      }

      .login-container h2 {
          text-align: center;
          font-size: 2rem;
          margin-bottom: 20px;
          color: #2c3e50;
      }

      .login-container .field {
          margin-bottom: 20px;
      }

      /* Input and Select Styling */
      .login-container select,
      .login-container input[type="email"],
      .login-container input[type="password"],
      .login-container input[type="text"] {
          width: 100%;
          padding: 10px;
          font-size: 1rem;
          border: 1px solid #ccc;
          border-radius: 5px;
          margin-top: 5px;
      }

      /* Checkbox Styling */
      .login-container .field input[type="checkbox"] {
          margin-right: 10px;
      }

      /* Submit Button Styling */
      .login-container .actions input[type="submit"] {
          background-color: #3498db;
          color: white;
          padding: 10px 20px;
          font-size: 1.1rem;
          border-radius: 5px;
          border: none;
          cursor: pointer;
          width: 100%;
      }

      .login-container .actions input[type="submit"]:hover {
          background-color: #2980b9;
      }

      /* Background and Layout Adjustments */
      body.login-page,
      body.signup-page {
          display: flex;
          align-items: center;
          justify-content: center;
          background: url('<%= asset_path("loginbackground.jpg") %>') no-repeat center center fixed;
          background-size: cover;
          min-height: 100vh;
          margin: 0;
      }

      /* Signup Page Specific Layout */
      body.signup-page .login-container {
          max-width: 750px;
      }

      body.signup-page .form-sections {
          display: flex;
          justify-content: space-between;
          gap: 20px;
      }

      /* Fieldset Styling */
      body.signup-page .login-container fieldset {
          border: 1px solid #ccc;
          padding: 20px;
          margin-bottom: 20px;
          border-radius: 5px;
          flex: 1;
      }

      body.signup-page .login-container legend {
          font-size: 1.2rem;
          font-weight: bold;
          color: #2c3e50;
          margin-bottom: 10px;
      }
    </style>
</head>
<body class="signup-page">

  <div class="login-container">
    <h2>Sign up</h2>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="form-sections">
        <!-- Section 1: Personal Information -->
        <fieldset>
          <legend>Personal Information</legend>

          <div class="field">
            <%= f.label :full_name %><br />
            <%= f.text_field :full_name %>
          </div>

          <div class="field">
            <%= f.label :phone_number %><br />
            <%= f.text_field :phone_number %>
          </div>

          <div class="field">
            <%= f.label :organization_type %><br />
            <%= f.select :organization_type, [
              'Food retailer',
              'Restaurant',
              'Truck driver',
              'Food pantry / soup kitchen',
              'Food processors',
              'Farm',
              'Individual (not associated with an organization)'
            ], { prompt: 'Select your organization type' } %>
          </div>

          <div class="field">
            <%= f.label :location %><br />
            <%= f.text_field :location %>
          </div>
        </fieldset>

        <!-- Section 2: Account Information -->
        <fieldset>
          <legend>Account Information</legend>

          <div class="field">
            <%= f.label :email %><br />
            <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
          </div>

          <div class="field">
            <%= f.label :password %>
            <% if @minimum_password_length %>
            <em>(<%= @minimum_password_length %> characters minimum)</em>
            <% end %><br />
            <%= f.password_field :password, autocomplete: "new-password" %>
          </div>

          <div class="field">
            <%= f.label :password_confirmation %><br />
            <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
          </div>

          <div class="field">
            <%= f.label :contact_preference, "Preferred contact method" %><br />
            <%= f.radio_button :contact_preference, "text" %> Text
            <%= f.radio_button :contact_preference, "email" %> Email
          </div>
        </fieldset>
      </div>

      <div class="actions">
        <%= f.submit "Sign up" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</body>
</html>
